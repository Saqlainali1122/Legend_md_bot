name: LEGEND BOY HACKER 
services:
  - name: LEGEND BOY HACKER-service
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
